# AI Agent Education Platform Requirements
# Updated: 2024-09-10

# Core Framework
fastapi==0.115.14
uvicorn[standard]==0.35.0

# Database
SQLAlchemy>=2.0.0,<3.0
alembic==1.16.2
# PostgreSQL support (primary database)
psycopg2-binary==2.9.10
# SQLite support (development only - optional)
# sqlite3 is included with Python standard library

# Authentication & Security
passlib[bcrypt]==1.7.4
bcrypt>=3.2.0,<4.0.0
python-jose[cryptography]==3.5.0
python-multipart==0.0.20
# OAuth dependencies
httpx>=0.28.1
requests>=2.31.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0

# AI Services
openai>=1.97.1

# LangChain Framework
langchain==0.3.7
langchain-openai==0.2.8
langchain-community==0.3.7
langchain-postgres==0.0.12
langchain-experimental==0.3.4

# Vector Database & Embeddings
pgvector>=0.2.5,<0.3.0

# Caching & Session Management
redis==5.2.0

# HTTP & Async
aiohttp==3.10.11
aiofiles==24.1.0

# PDF Processing
# Using LlamaParse API for all PDF parsing (no local dependencies needed)

# Configuration & Environment
python-dotenv==1.1.1
pydantic==2.11.7
pydantic-settings==2.6.1

# Development & Testing (optional)
pytest==8.4.1
pytest-asyncio==0.24.0

# Database Admin Interface (optional)
flask==3.0.3
flask-admin==1.6.1

# Additional utilities
schedule==1.2.2

# Scientific Computing
numpy>=1.26.0,<2.0.0  # Used in vector_store.py for array operations, compatible with LangChain
scikit-learn>=1.7.0,<2.0.0  # Used in embedding_service.py for cosine similarity calculations

# Note: The following are built-in Python modules and don't need to be installed:
# hashlib, base64, pickle, asyncio, json, datetime, time, os, sys, 
# logging, subprocess, re, string, unicodedata 

